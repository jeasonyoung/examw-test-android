<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.examw.test" android:versionCode="1"  android:versionName="1.0.0" >
    <!-- 设定多分辨率支持[能得到正确的屏幕尺寸] -->
	<supports-screens
            android:smallScreens="true"
            android:normalScreens="true"
            android:largeScreens="true"
            android:resizeable="true"
            android:anyDensity="true" />
	
	<!-- 支持的androidSDK版本 -->
    <uses-sdk android:minSdkVersion="8" android:targetSdkVersion="22" />
    
    <!-- 权限申明 -->
    
	<!-- 接入网络 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 访问网络状态 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 访问wifi状态 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 读写SD卡 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
	<!-- 获取mac地址作为用户的备用唯一标识 -->
    <!-- 获取手机设备码 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 系统设置 -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <!-- 震动 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- QQ、QQ空间所需权限 -->
	<uses-permission android:name="android.permission.GET_TASKS" />  
	
    <application android:name=".app.AppContext" android:allowBackup="true"
        android:icon="@drawable/app_logo" android:label="@string/app_name" android:theme="@style/AppTheme" >
        <!-- 启动入口 -->
        <activity android:name=".app.AppStart" android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 引导界面  -->
        <activity android:name=".ui.GuideActivity" 
            android:screenOrientation="portrait" />
        <!-- 主界面   -->
        <activity android:name=".ui.MainActivity" 
            android:launchMode="singleTask"
            android:screenOrientation="portrait" />
        <!-- 登录界面 -->
        <activity android:name=".ui.LoginActivity"
            android:screenOrientation="portrait" />
        <!-- 注册界面 -->
        <activity android:name=".ui.RegisterActivity"
            android:screenOrientation="portrait" />
        <!-- 选择科目界面 -->
        <activity android:name=".ui.ChooseSubjectActivity"
            android:screenOrientation="portrait" />
        <!-- 模拟考试界面  -->
        <activity android:name=".ui.SimulateActivity"
            android:screenOrientation="portrait" />
         <!-- 试卷介绍页面  -->
        <activity android:name=".ui.PaperInfoActivity"
            android:screenOrientation="portrait" />
        <!-- 试卷考试界面 -->
         <activity android:name=".ui.PaperDoPaperActivity"
            android:screenOrientation="portrait" />
        <!-- 答题卡界面  -->
         <activity android:name=".ui.AnswerCardActivity"
            android:screenOrientation="portrait" />
        <!-- 章节练习界面  -->
        <activity android:name=".ui.ChapterActivity"
            android:screenOrientation="portrait" />
        <!-- 章节练习界面  -->
        <activity android:name=".ui.ChapterDetailActivity"
            android:screenOrientation="portrait" />
        <!-- 每日一练界面  -->
        <activity android:name=".ui.DailyActivity"
            android:screenOrientation="portrait" />
        <!--错题,收藏的练习界面 -->
        <activity android:name=".ui.PaperDoPracticeActivity"
            android:screenOrientation="portrait" />
        <!-- 知识点界面 -->
        <activity android:name=".ui.KnowledgeActivity"
            android:screenOrientation="portrait" />
         <!-- 知识点详细界面 -->
        <activity android:name=".ui.KnowledgeDetailActivity"
            android:screenOrientation="portrait" />
        <!-- 论坛界面 -->
        <activity android:name=".ui.ForumActivity"
            android:screenOrientation="portrait" />
        <!-- 考试指南界面 -->
        <activity android:name=".ui.ExamInfoActivity"
            android:screenOrientation="portrait" />
        <!-- 考试记录界面  -->
        <activity android:name=".ui.PaperRecordActivity"
            android:screenOrientation="portrait" />
        <!-- 关于应用界面  -->
        <activity android:name=".ui.AboutActivity"
            android:screenOrientation="portrait" />
        <!-- 隐私协议界面 -->
        <activity android:name=".ui.PrivacyAgreementActivity"
            android:screenOrientation="portrait" />
        <!-- 时间设置界面 -->
        <activity android:name=".ui.SetTimeActivity"
            android:screenOrientation="portrait" />
        <!-- 同步界面 -->
        <activity android:name=".ui.SyncActivity"
            android:screenOrientation="portrait" />
        <!-- 意见界面 -->
        <activity android:name=".ui.FeedBackActivity"
            android:screenOrientation="portrait" />
        <!-- 数据导入界面 -->
        <activity android:name=".ui.ImportDataActivity"
            android:screenOrientation="portrait" />
        <!-- 图片缩放界面 -->
        <activity android:name=".ui.ImageZoomActivity"
            android:launchMode="singleTask"
            android:screenOrientation="portrait" />
        <activity android:name=".ui.ImageTextViewActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".ui.WebViewActivity"
            android:screenOrientation="portrait" />
        <!-- 注册码激活  -->
         <activity android:name=".ui.RegisterCodeActivity"
            android:screenOrientation="portrait" />
         
        <!-- 校验用户 -->
        <meta-data android:value="digest" android:name="username" />
        <meta-data android:value="7F522E56248B559B2F443C8EC310F0E7" android:name="password" />
        <!-- 终端号  终端号为数字, 前面要加\0才能转成字符串-->
        <meta-data android:value="\04" android:name="terminalId" />
        <!-- CLIENTKEY -->
        <meta-data android:value="U8z2D0O5s7Li1Q3y4k6g" android:name="clientKey" />
        <!-- 产品ID -->
        <meta-data android:value="c36f8985-544b-4524-909f-fef638f710b6" android:name="productId" />
        <!-- 论坛地址  -->
        <meta-data android:value="http://bbs.examw.com/forum-47-1.html" android:name="bbsUrl" />
        <!-- 报考指南地址 -->
        <meta-data android:value="http://m.examw.com/jzs1/zhinan/" android:name="infoUrl" />
    </application>
</manifest>
