<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.examw.test" android:versionCode="1" android:versionName="1.0" >
    
    <!-- 设置多分辨率支持[能得到正确得屏幕尺寸] -->
    <supports-screens android:smallScreens="true" android:normalScreens="true" android:largeScreens="true" android:anyDensity="true"/>
    
    <!-- 设置支持的SDK版本 -->
    <uses-sdk  android:minSdkVersion="8" android:targetSdkVersion="22"/>
    
    <!-- 设置权限 -->
    <uses-permission android:name="android.permission.INTERNET"/><!-- 接入网络 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/><!-- 访问网络状态 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/><!-- 访问Wi-Fi状态 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/><!-- 读写SD卡 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/><!-- 读取手机状态 -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS"/><!-- 系统设置 -->
    <uses-permission android:name="android.permission.VIBRATE"/><!-- 震动 -->
    <uses-permission android:name="android.permission.GET_TASKS"/><!-- QQ,QQ空间所需权限 -->
    
    <!-- 应用配置 -->
    <application android:allowBackup="true" android:icon="@drawable/app_icon" 
        	android:label="@string/app_name" android:theme="@style/AppTheme" >
        	
        	<!-- 欢迎界面 -->
        	
        
        
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
